---
name: capability-preservation-baseline
description: Use when generating the five supported verification document artifacts in the capability-preservation fixture.
---

# Capability Preservation Baseline

The executable supports `SRS`, `SDD_DETAIL`, `STP`, `STD`, and `STR` document outputs.
