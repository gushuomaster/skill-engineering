# Rule Governance Contract

The internal governance engine alone creates decisions. Findings are adjudicated as `KEEP`, `MERGE`, `MOVE`, or `DELETE`; detectors and external auditors may recommend an action but cannot apply it. Consolidate duplicates and preserve only rules with a stable, testable contract.

