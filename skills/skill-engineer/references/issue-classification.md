# Issue Classification Contract

Codex classifies findings across requirement status, defect class, and persistence scope and supplies a `DecisionRecord`. Distinguish stable capability invariants from `TASK_LOCAL_PREFERENCE`; the latter is never persisted. Fixes require Codex-authored root-cause analysis. `INSUFFICIENT_EVIDENCE` records the limitation and cannot justify a new Prompt Rule. The Engine validates record consistency only.
