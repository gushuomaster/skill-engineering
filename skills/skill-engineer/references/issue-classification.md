# Issue Classification Contract

Classify findings across requirement status, defect class, and persistence scope. Distinguish stable capability invariants from `TASK_LOCAL_PREFERENCE`; the latter is never persisted. Fixes require root-cause analysis. `INSUFFICIENT_EVIDENCE` records the limitation and cannot justify a new Prompt Rule.

