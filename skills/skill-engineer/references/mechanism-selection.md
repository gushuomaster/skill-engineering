# Mechanism Selection Contract

Choose the smallest durable mechanism and record rejected alternatives. The preference order is implementation fix, regression test, schema or validator, environment detection or tooling, workflow refactor, merge or generalize an existing invariant, then a new SKILL.md rule. A Prompt Rule requires evidence that stronger mechanisms cannot enforce the requirement.
