# Mechanism Selection Contract

Codex chooses the smallest durable mechanism and records every selected and rejected alternative. Consider implementation fixes, regression tests, schemas or validators, environment tooling, workflow changes, references, project instructions, configuration, and Skill instructions according to the actual cause. A Prompt Rule requires evidence that stronger mechanisms cannot enforce the requirement. The Engine rejects unknown, overlapping, or incomplete declarations but never chooses from this list.
